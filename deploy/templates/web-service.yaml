apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-example.fullname" . }}-web
  labels:
    {{- include "k8s-example.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "k8s-example.fullname" . }}-web
  ports:
    - port: 80
      targetPort: {{ .Values.web.port }}
      name: http
